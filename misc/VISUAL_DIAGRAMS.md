# Visual Flowcharts & Diagrams

Visual explanations of how your app works.

---

## 🏗️ ARCHITECTURE DIAGRAM

```
┌─────────────────────────────────────────────────────────────────┐
│                        YOUR COMPUTER                            │
│                                                                 │
│  ┌─────────────────────────┐      ┌────────────────────────┐   │
│  │   BROWSER (Port 3000)   │      │  PYTHON (Port 5000)    │   │
│  │                         │      │                        │   │
│  │  ┌─────────────────┐    │      │  ┌──────────────────┐  │   │
│  │  │   Website UI    │    │      │  │   Flask Server   │  │   │
│  │  │                 │    │      │  │                  │  │   │
│  │  │ [Home Page]     │    │      │  │ Receives requests│  │   │
│  │  │ [Analytics]     │    │◄────┐├──┤ Calls OpenAI     │  │   │
│  │  │ [Text Analysis] │────┤────►││  │ Queries database │  │   │
│  │  │ [Infographics]  │    │      │  │ Returns results  │  │   │
│  │  │                 │    │      │  │                  │  │   │
│  │  │ [Send button]   │    │      │  └──────────────────┘  │   │
│  │  └─────────────────┘    │      │         ↓              │   │
│  │                         │      │  ┌──────────────────┐  │   │
│  │    (React App)          │      │  │   Database       │  │   │
│  └─────────────────────────┘      │  │  (SQLite/PG)     │  │   │
│                                   │  │                  │  │   │
│                                   │  │ ├─ orders table  │  │   │
│                                   │  │ ├─ customers     │  │   │
│                                   │  │ └─ products      │  │   │
│                                   │  └──────────────────┘  │   │
│                                   │         ↑              │   │
│                                   │  ┌──────────────────┐  │   │
│                                   │  │   OpenAI API     │  │   │
│                                   │  │                  │  │   │
│                                   │  │ - Generates SQL  │  │   │
│                                   │  │ - Summarizes     │  │   │
│                                   │  └──────────────────┘  │   │
│                                   │                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 REQUEST/RESPONSE FLOW

### User Asks a Question

```
1. USER ACTION
   ┌──────────────────────────┐
   │ Opens Website            │
   │ http://localhost:3000    │
   └──────────────┬───────────┘
                  │
                  ▼
2. USER TYPES & SENDS
   ┌──────────────────────────┐
   │ "What are my top         │
   │  products?"              │
   │                          │
   │ [         Send ]         │
   └──────────────┬───────────┘
                  │
                  ▼
3. BROWSER SENDS HTTP REQUEST
   ┌──────────────────────────────────┐
   │ POST /api/text/analyze           │
   │                                  │
   │ {                                │
   │   "user_message": "What are      │
   │   my top products?"              │
   │ }                                │
   └──────────────┬────────────────────┘
                  │
                  │ HTTP over localhost
                  │
                  ▼
4. PYTHON RECEIVES REQUEST
   ┌──────────────────────────┐
   │ app.py                   │
   │                          │
   │ @app.route('/api/...')   │
   │ def analyze_text():      │
   │   ↓                      │
   │   Gets the message       │
   └──────────────┬───────────┘
                  │
                  ▼
5. ASK OPENAI FOR QUESTIONS
   ┌──────────────────────────────┐
   │ OpenAI API Call              │
   │                              │
   │ "Given: 'What are my top     │
   │  products?'                  │
   │                              │
   │  Generate 5-10 questions"    │
   │                              │
   │ OpenAI Responds:             │
   │ [                            │
   │   "Which products are most   │
   │    popular by count?",       │
   │   "What is total revenue     │
   │    by product?",             │
   │   "Which product has highest │
   │    margin?",                 │
   │   ...                        │
   │ ]                            │
   └──────────────┬───────────────┘
                  │
                  ▼
6. FOR EACH QUESTION, GENERATE & EXECUTE SQL
   ┌─────────────────────────────────────┐
   │ Question 1:                         │
   │ "Which products are most popular?" │
   │                                     │
   │ OpenAI generates SQL:               │
   │ "SELECT product,                    │
   │  COUNT(*) as count                  │
   │  FROM orders                        │
   │  GROUP BY product                   │
   │  ORDER BY count DESC"               │
   │                                     │
   │ Execute on database ↓               │
   │                                     │
   │ Results:                            │
   │ {product: 'Laptop', count: 3}       │
   │ {product: 'Mouse', count: 2}        │
   │ {product: 'Keyboard', count: 1}     │
   │                                     │
   │ Question 2: (same process)          │
   │ Question 3: (same process)          │
   │ ...                                 │
   └──────────────┬──────────────────────┘
                  │
                  ▼
7. ASK OPENAI TO SUMMARIZE
   ┌──────────────────────────────┐
   │ OpenAI API Call              │
   │                              │
   │ "Read these query results:   │
   │  {results}                   │
   │                              │
   │  Create a 3-sentence         │
   │  executive summary"          │
   │                              │
   │ OpenAI Responds:             │
   │ "Your top product by         │
   │  popularity is Laptops with  │
   │  3 sales. This is followed   │
   │  by Mice (2) and Keyboards   │
   │  (1). Overall, electronics   │
   │  dominate your sales."       │
   └──────────────┬───────────────┘
                  │
                  ▼
8. SEND RESPONSE BACK TO BROWSER
   ┌──────────────────────────────────┐
   │ HTTP Response (JSON)             │
   │                                  │
   │ {                                │
   │   "success": true,               │
   │   "summary": "Your top product..│
   │   "questions": [                 │
   │     "Which products...",         │
   │     "What is total revenue..."   │
   │   ],                             │
   │   "results": [...]               │
   │ }                                │
   └──────────────┬────────────────────┘
                  │
                  │ HTTP over localhost
                  │
                  ▼
9. BROWSER RECEIVES RESPONSE
   ┌──────────────────────────┐
   │ React App State Updates   │
   │                          │
   │ setChatHistory([         │
   │   { sender: 'user',      │
   │     text: "What are..." },│
   │   { sender: 'bot',       │
   │     text: "Your top      │
   │            product is..."│
   │   }                      │
   │ ])                       │
   └──────────────┬───────────┘
                  │
                  ▼
10. BROWSER DISPLAYS RESULT
   ┌──────────────────────────┐
   │ User: "What are my       │
   │        top products?"    │
   │                          │
   │ Bot: "Your top product   │
   │      by popularity is    │
   │      Laptops with 3      │
   │      sales..."           │
   │                          │
   │ Generated Questions:     │
   │ 1. Which products...     │
   │ 2. What is total...      │
   │                          │
   │ [Table of results]       │
   └──────────────────────────┘
```

---

## 📊 DATA FLOW DIAGRAM

```
                    BACKEND

┌─────────────────────────────────────────────────────┐
│                   app.py (Flask)                    │
│                                                     │
│  1. HTTP Request Arrives                            │
│     ├─ Route: /api/text/analyze                     │
│     ├─ Method: POST                                 │
│     └─ Body: {user_message: "..."}                  │
│                                                     │
│  2. Function: analyze_text()                        │
│     ├─ Extract message                              │
│     ├─ Call OpenAI for questions                    │
│     │  └─ Get 5-10 questions back                   │
│     │                                               │
│     ├─ Loop through each question:                  │
│     │  ├─ Ask OpenAI to generate SQL                │
│     │  ├─ Execute SQL on database                   │
│     │  └─ Get results                               │
│     │                                               │
│     ├─ Call OpenAI to summarize                     │
│     │  └─ Get summary text                          │
│     │                                               │
│     └─ Return JSON response                         │
│                                                     │
│  3. HTTP Response Sent Back                         │
│     └─ Body: {success, summary, questions, ...}     │
│                                                     │
└─────────────────────────────────────────────────────┘
         │              │              │
         │              │              │
         ▼              ▼              ▼
    ┌─────────┐   ┌──────────┐   ┌──────────────┐
    │ OpenAI  │   │Database  │   │Logging       │
    │ API     │   │(SQLite)  │   │(Terminal)    │
    │ Calls   │   │Queries   │   │Messages      │
    └─────────┘   └──────────┘   └──────────────┘
```

---

## 🔄 TEXT ANALYSIS PROCESS

```
┌────────────────────────────────────────────────────┐
│ User Question: "What are my top products?"         │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ STEP 1: Generate Questions                         │
│ ─────────────────────────────────────              │
│ OpenAI Prompt:                                     │
│ "Given this question, generate 5-10 questions     │
│  about the data that could answer it"             │
│                                                    │
│ Response:                                          │
│ 1. Which products have highest sales?             │
│ 2. What is average price per product?             │
│ 3. Which product has highest profit margin?       │
│ 4. What is the sales trend per product?           │
│ 5. How many times was each product sold?          │
│ 6. What is total revenue by product?              │
│ 7. Which product is most profitable?              │
│ 8. What products are in the top 3?                │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ STEP 2: Execute Each Question                      │
│ ─────────────────────────────────────              │
│ For Q1: "Which products have highest sales?"      │
│         ├─ OpenAI generates SQL:                   │
│         │  SELECT product, SUM(amount) as sales    │
│         │  FROM orders                             │
│         │  GROUP BY product                        │
│         │  ORDER BY sales DESC                     │
│         │                                          │
│         └─ Execute on database                     │
│            Results:                                │
│            Laptop: $3300                           │
│            Monitor: $300                           │
│            Keyboard: $100                          │
│            Mouse: $100                             │
│                                                    │
│ For Q2: "What is average price?"                  │
│         ├─ OpenAI generates SQL: ...               │
│         └─ Results: Avg price: $392.85             │
│                                                    │
│ For Q3, Q4, Q5... (same process)                  │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ STEP 3: Summarize All Results                      │
│ ─────────────────────────────────────              │
│ OpenAI Prompt:                                     │
│ "Here are results from 8 questions about products │
│  Create a 3-4 sentence executive summary"        │
│                                                    │
│ Results Summary:                                   │
│ {"summary": "Your top-selling product is Laptops, │
│   which account for $3,300 in revenue (73% of     │
│   total sales). The average product price is      │
│   $392.85, with Laptops being the highest-margin  │
│   item."}                                          │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ Return to User                                      │
│ ─────────────────────────────────────              │
│ In Browser:                                        │
│                                                    │
│ User: What are my top products?                   │
│                                                    │
│ Bot: Your top-selling product is Laptops, which   │
│      account for $3,300 in revenue (73% of total  │
│      sales). The average product price is $392.85,│
│      with Laptops being the highest-margin item.  │
│                                                    │
│ Generated Questions:                               │
│ 1. Which products have highest sales?             │
│ 2. What is average price per product?             │
│ ... (8 questions total)                           │
└────────────────────────────────────────────────────┘
```

---

## 📈 GRAPH GENERATION PROCESS

```
┌────────────────────────────────────────────────────┐
│ User Request: "Show me revenue by month"          │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ STEP 1: Generate Visualization Questions           │
│ ─────────────────────────────────────              │
│ Response:                                          │
│ How has revenue changed over time? [line]         │
│ What's the revenue distribution? [pie]            │
│ Which month had highest revenue? [bar]            │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ STEP 2: For Each Visualization                     │
│ ─────────────────────────────────────              │
│ Q1: "How has revenue changed over time?"          │
│                                                    │
│ Generate SQL:                                      │
│ SELECT strftime('%Y-%m', date) as month,          │
│        SUM(amount) as revenue                      │
│ FROM orders                                        │
│ GROUP BY month                                     │
│ ORDER BY month                                     │
│                                                    │
│ Execute: Get data                                 │
│ ┌────────┬─────────┐                              │
│ │ month  │ revenue │                              │
│ ├────────┼─────────┤                              │
│ │ 2024-01│ 1150    │                              │
│ │ 2024-02│ 1500    │                              │
│ │ 2024-03│ 1150    │                              │
│ └────────┴─────────┘                              │
│                                                    │
│ Create Line Chart:                                │
│ $                                                  │
│ 1500 │        ╱╲                                  │
│ 1400 │       ╱  ╲                                 │
│ 1300 │      ╱    ╲                                │
│ 1200 │     ╱      ╲                               │
│ 1100 │    ╱        │                              │
│ 1000 └───────────────                            │
│      Jan   Feb   Mar                              │
│                                                    │
│ Q2, Q3: (same process, different chart types)    │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────┐
│ Return Charts to Browser                           │
│ ─────────────────────────────────────              │
│ [Line Chart showing revenue trend]                │
│ [Pie Chart showing distribution]                  │
│ [Bar Chart showing by month]                      │
│                                                    │
│ All with REAL DATA from your database!           │
└────────────────────────────────────────────────────┘
```

---

## 🔗 FILE DEPENDENCIES

```
Browser Opens
     │
     ▼
index.html ◄─────────────────────────────────┐
     │ (loads this script)                   │
     ▼                                       │
index.js (startup)                          │
     │                                       │
     ├─ imports './index.css' ──────┐       │
     │   └─ Global styles           │       │
     │                              │       │
     ├─ imports './App'             │       │
     │   │                          │       │
     │   ▼                          │       │
     │ App.jsx (main component)     │       │
     │   │                          │       │
     │   ├─ imports 'api.js'        │       │
     │   │   └─ Talks to Python     │       │
     │   │                          │       │
     │   ├─ imports 'styles.css'    │       │
     │   │   └─ More styling        │       │
     │   │                          │       │
     │   └─ Contains 4 pages:       │       │
     │       ├─ HomePage            │       │
     │       ├─ AnalyticsPage       │       │
     │       ├─ TextAnalysisPage    │       │
     │       └─ InfographicsPage    │       │
     │                              │       │
     └─ renders to: ─────────────────┘
        document.getElementById('root')
             │
             ▼
       (div id="root" in HTML)
             │
             ▼
       WEBSITE DISPLAYS!
```

---

## 🔐 SECURITY FLOW

```
User's Computer
├─ index.html ✓ (safe - just HTML)
├─ App.jsx ✓ (safe - no secrets)
├─ api.js ✓ (safe - no API keys)
│
└─ .env ✗ (NEVER share this!)
   ├─ OPENAI_API_KEY (secret!)
   └─ DB_URI (connection string)

When you send request to backend:
1. Browser sends: {user_message: "..."}
2. Python receives it
3. Python uses secrets from .env
4. Backend calls OpenAI (securely)
5. Backend queries database (securely)
6. Returns only data to browser
7. Browser never sees secrets!
```

---

## 📱 CLIENT-SERVER COMMUNICATION

```
CLIENT (Browser)          NETWORK          SERVER (Python)
─────────────────────────────────────────────────────────

                    HTTP Request
          POST /api/text/analyze
          Content-Type: application/json
          ────────────────────────→
          {
            "user_message": "What are
             my top products?"
          }

                          ↓ (Server processes)

                    HTTP Response
          ←─────────────────────────
          Content-Type: application/json
          {
            "success": true,
            "summary": "...",
            "questions": [...],
            "results": [...]
          }

          ↓ (Browser displays)
```

---

These diagrams show exactly how each piece connects and communicates!

